name: Recolector Horarios 141

on:
  schedule:
    - cron: '*/15 * * * *'  # Cada 15 min
  workflow_dispatch:

jobs:
  recolectar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Chrome
        uses: nanasess/setup-chromedriver@v2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependencias
        run: pip install selenium pytz

      - name: Recolectar horarios
        run: python recolector.py

      - name: Commit y Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p data
          mv horarios.txt data/horarios-141-$(date +'%Y%m%d-%H%M%S').txt
          git add data/horarios-141.txt
          git commit -m "🚌 Línea 141: $(head -1 data/horarios-141.txt | cut -d' ' -f4-)" || echo "Sin cambios"
          git push

      - name: Subir Artifact
        uses: actions/upload-artifact@v4
        with:
          name: horarios-141-${{ github.run_number }}
          path: data/horarios-141.txt
          retention-days: 7

